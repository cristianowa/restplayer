import unittest
#TODO: for now we don't run the webservice from here
import requests

class MyTestCase(unittest.TestCase):
    def test_start(self):
        self.assertTrue(True)
    def test_pause(self):
        self.assertTrue(True)
    def test_stop(self):
        self.assertTrue(True)

if __name__ == '__main__':
    unittest.main()
